# Database
DATABASE_URL="postgresql://naro_admin:NaroFashion2026@localhost:5432/naro_fashion?schema=public"

# JWT — REQUIRED in production. The API refuses to start if either is missing
# or shorter than 32 chars when NODE_ENV=production. In dev a per-process
# random fallback is used so leaked dev tokens are useless across restarts.
# Generate with: openssl rand -hex 48
JWT_SECRET=""
JWT_REFRESH_SECRET=""
JWT_ACCESS_EXPIRES="15m"
JWT_REFRESH_EXPIRES="7d"

# Cloudinary
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""

# Selcom Payment Gateway (https://developers.selcom.net/)
SELCOM_API_KEY=""
SELCOM_API_SECRET=""
SELCOM_VENDOR=""
SELCOM_BASE_URL="https://apigw.selcom.net/v1"

# ClickPesa (Mixx by YAS / Tigo Pesa) — per-tenant credentials live in
# PaymentMethod.integrationParams (clientId, apiKey, checksumSecret). These
# are shared defaults used by the provider client + reconciliation cron.
CLICKPESA_BASE_URL="https://api.clickpesa.com/third-parties"
CLICKPESA_TOKEN_TTL_MINUTES="55"
CLICKPESA_POLL_INTERVAL_SECONDS="30"
CLICKPESA_RECONCILE_CUTOFF_MINUTES="5"

# Flutterwave
FLUTTERWAVE_PUBLIC_KEY=""
FLUTTERWAVE_SECRET_KEY=""
FLUTTERWAVE_ENCRYPTION_KEY=""
FLUTTERWAVE_WEBHOOK_SECRET=""

# Africa's Talking (SMS)
AT_API_KEY=""
AT_USERNAME=""
AT_SENDER_ID=""

# Email (SMTP)
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="noreply@narofashion.co.tz"

# App URLs
STOREFRONT_URL="http://localhost:3000"
ADMIN_URL="http://localhost:3001"
API_URL="http://localhost:4000"

# Google OAuth
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Facebook OAuth
FACEBOOK_APP_ID=""
FACEBOOK_APP_SECRET=""

# Instagram Graph API
INSTAGRAM_ACCESS_TOKEN=""
INSTAGRAM_BUSINESS_ACCOUNT_ID=""
